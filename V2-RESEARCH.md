# Tibo Watch V2 产品与技术方向

> 状态：已批准实施；V2 个人体验测试候选版开发完成  
> 日期：2026-08-11  
> 依据：V1 真实使用反馈、现有 1,684 条冻结数据集、OpenAI/DeepSeek/Electron/飞书官方能力。  
> 约束：不重复搜索或抓取 FxAPI 历史样本。

## 1. V2 定位

V2 仍是 Windows 本地单用户桌面应用，无中心服务器、账号系统、云同步或遥测。产品从 V1 的工程验证大屏升级为精致、可信、可持续运行的个人监控与 Codex 恢复助手。

核心目标：

1. 可靠监控 Tibo 的公开发言，过滤无效回复。
2. 清楚展示最近一次重置、七天周期基线和新的重置信号。
3. 允许规则独立触发通知、webhook 与 Codex 会话恢复。
4. 可选 AI 只增强语义判断，不成为基础能力的前置条件。
5. 可选 OpenAI 账户授权只增强额度判断，不成为规则或 AI 的前置条件。
6. 让用户恢复原有 Codex 会话，而不是创建缺少上下文的新任务。

## 2. 视觉与信息架构

### 2.1 设计语言

- 参考 Codex Desktop 的克制桌面设计，白色为主。
- 使用浅灰页面背景、白色内容面、细边框、低强度阴影和单一强调色。
- 避免廉价霓虹、深色“数据战情室”、大面积渐变和过度卡片化。
- 信息密度由排版与留白控制；诊断数据不与核心判断争夺注意力。
- 保留键盘导航、清晰焦点、缩放适配和减少动画设置。

### 2.2 三个侧边栏入口

#### 监控

监控页包含：

- 运行状态、数据源健康、最近检查时间。
- 最近一次实际重置时间。
- 七天周期基线：最近一次实际重置时间加 7 天。
- 新信号预测：仅在规则候选或 AI 判断出现后展示并优先于周期基线。
- 重置日期热力图和次数统计。
- 最新消息：只显示规则筛选后的有效候选，按时间倒序。

周期基线必须标明是历史计划外推，不伪装成实时确定预测。没有新信号时显示“尚无新信号”，同时保留七天基线。

#### 历史消息和审计

- 历史消息按最新时间倒序。
- 默认隐藏无关回复和无关原创帖，可由用户主动显示仍在保留期内的记录。
- 同一页面可切换审计视图：采集 → 规则 → AI → 事件 → 通知/webhook → Codex 恢复。
- 每一步显示时间、版本、输入哈希、结果和失败原因。
- 原始 JSON 放在可展开的高级区域，不作为默认界面。

#### 设置

设置按能力解耦：

1. 数据源。
2. 判断引擎。
3. OpenAI/Codex 账户。
4. Codex 会话恢复。
5. 通知与 webhook。
6. 存储与缓存。
7. 诊断、导出与版本。

## 3. 时间与重置模型

V2 区分三个事实：

- `lastObservedResetAt`：最近一次实际观测到的重置。
- `baselineNextResetAt`：`lastObservedResetAt + 7 天`。
- `signalPrediction`：规则或 AI 根据新消息生成的动态预计窗口。

展示规则：

1. 实际重置发生后立即更新 `lastObservedResetAt`。
2. 同时重新计算七天周期基线。
3. 没有新信号时保持该基线，不随当前时间滚动。
4. 出现候选信号后，优先展示动态预计窗口，但仍保留基线作为对照。
5. 新的实际重置被观测后，清除过期动态窗口并进入下一周期。

## 4. AI 协议兼容与设置解耦

### 4.1 支持的三类协议

- Anthropic Messages API：`/v1/messages`。
- OpenAI Responses API：`/v1/responses`。
- OpenAI Chat Completions：`/v1/chat/completions`。

每个 Provider 独立配置：

- 协议类型。
- Base URL。
- 模型名称。
- API Key。
- 可选自定义请求头。
- 超时、最大重试和连接测试。

Key 与秘密请求头只保存到 Windows 凭据管理器。连接测试不发送真实帖子内容。Provider 负责请求与响应格式转换，领域层只接收统一的结构化 `AnalysisResult`。

### 4.2 规则与 AI 的关系

- 规则系统是始终可运行的基础能力。
- 未配置或无法调用 AI 时，规则仍可通知、触发 webhook 和恢复 Codex。
- AI 是规则候选后的增强判断，用于改善事件类型、影响范围、时间窗口和置信说明。
- AI 结果无效、超时或不可用时回到规则策略；必须在审计中明确标记降级。
- 规则版本和 AI Provider 配置完全独立。

## 5. 两个解耦维度

OpenAI 账户授权与 AI API 配置完全解耦：

| OpenAI 账户 | AI API | 可用行为                                                 |
| ----------- | ------ | -------------------------------------------------------- |
| 未授权      | 未配置 | 规则判断 → 通知/webhook/保守自动恢复；无法核实个人额度   |
| 已授权      | 未配置 | 规则判断 + 真实额度与预算门禁 → 通知/webhook/自动恢复    |
| 未授权      | 已配置 | 规则筛选 + AI 增强 → 通知/webhook/恢复；无法核实个人额度 |
| 已授权      | 已配置 | 规则 + AI + 真实额度门禁的完整策略                       |

任何一项缺失都不得关闭另一项基础能力。

## 6. Codex 会话恢复

### 6.1 官方能力边界

Codex App Server 提供：

- `thread/list`、`thread/read`：读取本地已有线程及最后状态。
- `thread/resume`：加载原线程。
- `turn/start`：在原上下文继续工作。
- `account/rateLimits/read`：读取已认证 Codex 用户的额度窗口。
- `account/rateLimits/updated`：接收额度变化。
- `account/rateLimitResetCredit/consume`：显式使用可用重置机会。

参考：[Codex App Server 官方文档](https://learn.chatgpt.com/docs/app-server)。App Server 当前仍有实验性边界，适配器必须做版本握手、能力探测和失败关闭。

真正未认证的用户不能读取账户额度。用户已经登录 Codex CLI/Desktop 时，App Server 可以复用本机身份，但 Tibo Watch 仍必须先取得用户明确授权，不能因为技术上可访问就静默读取。

### 6.2 恢复对象

- V2 恢复现有 Codex 线程，不建立独立任务队列。
- 用户必须对具体线程开启“允许恢复”。
- 保留原线程历史、工作目录、模型上下文和权限边界。
- 恢复消息固定表达“从上次中断处继续，先检查工作区状态，避免重复已完成步骤”。
- 不自动扩大 sandbox、网络或文件权限。

### 6.3 触发策略

#### 未授权账户额度

规则或 AI 观测到公开重置信号后，可以恢复用户已授权的线程。必须显示：

> 仅依据公开信号执行，无法核实你的个人 Codex 额度；工作可能再次因额度限制失败。

#### 已授权账户额度

公开信号与账户额度可以共同参与门禁。账户授权只增加额度证据，不改变规则/AI 是否可运行。

预算使用迟滞区间：

- 上限：使用比例达到用户阈值后，不启动新的恢复。
- 下限：使用比例降到用户阈值以下，才重新允许恢复。
- 中间区间保持原状态，避免反复启停。
- 额度上限只是一道“新恢复任务启动门禁”，不具备运行中任务控制权。
- 用户已经运行中的 Codex CLI、Codex Desktop、原有线程或其他程序，即使随后达到或超过额度上限，Tibo Watch 也不得暂停、停止、取消、终止进程或发送中断指令。
- 对运行中任务，Tibo Watch 只能记录最新额度状态并给出非阻断式提示；任务是否继续、暂停或终止始终由用户本人或 Codex 自身的额度机制决定。
- 该限制不是 V2 首版的临时约束，而是默认安全边界。未来如增加运行中任务控制，也必须作为独立能力另行设计并取得用户逐次明确授权，不能由预算阈值隐式触发。
- reset credit 永不自动消耗，必须由用户单独确认。

### 6.4 规则保底与 AI 升级

- 规则模式：允许通知和自动恢复，但采用更保守的延迟、并发和风险提示。
- AI 模式：规则先筛选，AI 提供更完整语义后按正常策略执行。
- AI 不可用时不阻断规则路径。
- 每次触发记录实际采用的是 `rule-only` 还是 `rule+ai`。

### 6.5 风险控制

- 全局自动恢复急停。
- 单线程开启、暂停和撤销。
- 每个信号生成幂等触发 ID，防止重复 resume。
- 重启后不补执行已经过期的触发。
- 同一时间默认只恢复一个线程；用户可在高级设置提高上限。
- Codex 等待批准时由 Codex 自身权限系统处理，Tibo Watch 不代替用户批准高风险操作。
- 记录触发来源、规则、AI、额度快照、线程、目录、权限、开始/完成状态和错误。
- 明确提示自动执行可能修改文件、调用外部服务并消耗额度，不承诺任务成功。

`codex exec` 是稳定的非交互入口，适合作为降级执行方式；它支持沙箱、JSONL 和结构化输出。[Codex 非交互模式](https://learn.chatgpt.com/docs/non-interactive-mode)

## 7. 通知、webhook 与用户扩展接口

通知事件类型必须显式区分：

- `rule_candidate`：规则候选，尚未经 AI 确认。
- `ai_confirmed`：AI 增强确认。
- `reset_observed`：实际重置已观测。
- `codex_resume_started`：开始恢复原线程。
- `codex_resume_waiting_approval`：Codex 等待用户批准。
- `codex_resume_completed`：恢复工作完成。
- `codex_resume_failed`：恢复失败或再次限流。

每种事件可分别启用 Windows、飞书和通用 HTTP webhook。通用 HTTP webhook 使用版本化 schema，允许用户开发自有接收器；必须包含事件类型、schema 版本、幂等键、发生时间和非秘密摘要。

飞书自然语言交互留到后续版本。V2 首版不接收“继续”等聊天命令，避免引入身份绑定、消息事件长连接和自然语言授权歧义。

## 8. 缓存与历史保留

- 无关回复：保留 7 天后清理。
- 无关原创帖和引用帖：保留 14 天后清理。
- 规则候选、AI 分析、确认事件、重置事实、通知和审计：长期保留。
- 不长期缓存图片、视频等媒体。
- 清理每天低优先级运行一次，不影响监控轮询。
- 清理使用单写入队列并更新可重建索引。
- 日志只记录删除类别、数量和时间，不记录已删除正文。
- 设置允许用户缩短期限或选择“仅保留候选”，不允许无提示地扩大为无限保留。

## 9. V2 实施阶段

### V2-0：交互原型与迁移基线

- 完成白色 Codex Desktop 风格的三页高保真原型。
- 使用 V1 真实本地数据验证信息层级。
- 建立 V1→V2 数据迁移、备份和回滚 fixture。

### V2-1：新壳与三页导航

- 侧边栏、监控、历史消息和审计、设置。
- 最近重置、七天基线、新信号预测和热力图。
- 历史倒序、默认过滤和详情抽屉。

### V2-2：AI Provider 解耦

- Messages、Responses、Chat Completions 三种适配器。
- 自定义 Base URL、模型、Key 和请求头。
- 统一 schema、连接测试、降级与审计。

### V2-3：触发器与 webhook

- 版本化事件总线。
- 规则、AI、重置和 Codex 恢复事件。
- 每事件类型独立渠道开关和用户扩展 webhook。

### V2-4：Codex 只读集成

- App Server 能力探测。
- 用户授权后读取线程、状态、额度和重置时间。
- 未授权模式只读取用户明确允许的本地会话元数据，不调用账户接口。

### V2-5：Codex 自动恢复

- 单线程授权、规则/AI 双策略、可选额度门禁。
- 预算上下限、幂等、急停、状态流和失败恢复。
- reset credit 仅手动确认。

### V2-6：缓存治理与发布

- 7/14 天保留策略、索引修复和空间统计。
- Windows 安装/升级/卸载、代码签名和 V1 回滚验收。
- 飞书自然语言交互不进入本阶段。

## 10. 进入实现前的审阅门禁

以下内容经用户明确批准后才进入实现：

- 三页侧边栏和白色视觉方向。
- 七天周期基线与动态信号的双层时间展示。
- 账户授权与 AI 配置的二维解耦。
- 恢复已有 Codex 线程，而不是创建任务队列。
- 规则保底、AI 升级、额度门禁可选。
- 预算额度只阻止新的自动恢复，绝不暂停或终止用户已在运行的程序和 Codex 任务。
- 无关回复 7 天、无关原创/引用 14 天。
- 飞书自然语言交互后置。

上述方向已于 2026-08-11 获得用户批准并进入实现。个人体验测试期间发现的问题继续以本文件的安全边界为验收依据。
